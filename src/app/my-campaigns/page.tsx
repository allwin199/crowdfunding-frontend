import FetchMyCampaigns from "@/components/campaigns/FetchMyCampaigns";

const page = () => {
    return (
        <main className="container mx-auto pt-20">
            <FetchMyCampaigns />
        </main>
    );
};

export default page;
